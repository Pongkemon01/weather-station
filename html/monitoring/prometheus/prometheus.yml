global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: iot_server
    static_configs:
      - targets: ["127.0.0.1:8000"]
    metrics_path: /metrics
